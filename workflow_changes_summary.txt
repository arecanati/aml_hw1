AML HW1 Workflow: Changes and Additions Log
===========================================

Project context
---------------
- Dataset used: UCI Cleveland heart disease subset (`data/raw/processed.cleveland.data`).
- Goal: end-to-end pipeline from raw data to evaluated predictive models.

Key updates completed
---------------------
1) Data and feature pipeline
- Created binary target `target_bin` (`target > 0`).
- Added missingness indicators: `ca_missing`, `thal_missing`.
- Added engineered features:
  - `maxhr_age_ratio`
  - `exercise_risk`
  - `thal_is_abnormal`
- Added short inline comments in Step 2 describing each engineered feature in plain language.

2) Modeling pipeline alignment
- Kept one stratified split policy (`test_size=0.2`, `random_state=SEED`, `stratify=y`).
- Step 5 now tunes `GaussianNB(var_smoothing)` over a small candidate grid and keeps the best result.
- Step 8 ablation now uses the same preprocessing policy as Steps 5/6 (train-only imputation + one-hot encoding).
- Step 8 Gaussian now reuses the tuned `var_smoothing` from Step 5, so Step 7 Gaussian accuracy and Step 8 post-engineered Gaussian accuracy are consistent.

3) Evaluation and reporting clarity
- Metrics reported consistently: Accuracy, Precision, Recall, F1, confusion matrices, classification reports.
- Step 8 remains a controlled pre-vs-post engineered feature comparison (not a separate leaderboard).

4) Code quality cleanup (no functional intent change)
- Reduced redundancy by adding shared helpers in the first code cell:
  - shared preprocessing helper
  - shared metric computation helper
  - shared metrics-print helper
- Reused helpers across Steps 5, 6, and 8 to avoid duplicated logic and reduce mismatch risk.

5) Environment/reproducibility
- Cleaned `requirements.txt` to a minimal pinned set required by this project.
- Added `.gitignore` and initialized/pushed repository to GitHub.

Current status
--------------
- Notebook is consistent end-to-end.
- Main comparison and ablation outputs now align methodologically.
- Project is committed and pushed to remote repository.
